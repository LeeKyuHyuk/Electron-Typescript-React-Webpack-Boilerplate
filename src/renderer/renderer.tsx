import * as React from 'react';
import * as ReactDOM from 'react-dom';

ReactDOM.render(
  <div className="app">
    <h1>Hello, Electron + TypeScript + React + Webpack Boilerplate!</h1>
  </div>,
  document.getElementById('root'),
);
